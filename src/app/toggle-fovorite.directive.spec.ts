import { ToggleFovoriteDirective } from './toggle-favorite.directive';

describe('ToggleFovoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleFovoriteDirective();
    expect(directive).toBeTruthy();
  });
});
